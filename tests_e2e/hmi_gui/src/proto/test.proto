syntax = "proto3";

// These Shares can be unique to this device.
// Test Request and Publish
message Share1 { // General device params
    fixed32 startingNumber = 1;
    fixed32 endingNumber = 2;
    fixed32 counter = 3; // Counts up each second from the starting number to the ending number
}